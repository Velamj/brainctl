#!/usr/bin/env python3
"""
brainctl — Unified agent memory CLI
"""
import sys, os

# Resolve symlinks to find the real repo location
_real_dir = os.path.dirname(os.path.realpath(__file__))
sys.path.insert(0, os.path.join(os.path.dirname(_real_dir), 'src'))

from agentmemory._impl import main
main()
